"""Step 2: Heuristic HTML parsing for extracting image URLs."""


def parse_html(html_content, base_url=None):
    """
    Parse HTML content and extract image URLs using heuristics.
    
    Args:
        html_content (str): HTML content to parse
        base_url (str, optional): Base URL for resolving relative URLs
        
    Returns:
        list: List of image URLs found in the HTML
    """
    pass
